# School Finder Scraper
A Python Flask web application that scrapes school data from Google Maps and exports it to Excel.

## Setup
1. Clone this repository.
2. Install dependencies: `pip install -r requirements.txt`
3. Run the app: `python app.py`
4. Access it at `http://127.0.0.1:5000`
